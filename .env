# Base configuration - DO NOT ADD ENVIRONMENT SPECIFIC VALUES HERE
# This file serves as a template and documentation for required environment variables
#
# Environment specific files:
# .env.local   - Local overrides for SSO and auth configuration
# .env.dev     - Development environment settings
# .env.prod    - Production environment settings (not tracked in git)

# Public info and no environment variables
# NO_VARS_AT_THE_MOMENT=

# Required environment variables (set these in appropriate .env.* files)
NODE_ENV=
NEXTAUTH_URL=
